# Modified files from STRetch install

Install

```sh
git clone https://github.com/Oshlack/STRetch.git
cd STRetch
git checkout 5405902
bash install.sh
```

Only files added or modified are included in this directory
